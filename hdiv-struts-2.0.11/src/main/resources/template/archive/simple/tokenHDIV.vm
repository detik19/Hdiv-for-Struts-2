#set( $hdivValue = $dataComposer.compose("$!{parameters.tokenNameField}", "$struts.htmlEncode($!parameters.name)", false) )
<input type="hidden" name="$!{parameters.tokenNameField}" value="$hdivValue"/>
#set( $hdivValue = $dataComposer.compose("$!parameters.name", "$struts.htmlEncode($!parameters.token)", false) )
<input type="hidden" name="$!parameters.name" value="$hdivValue"/>
